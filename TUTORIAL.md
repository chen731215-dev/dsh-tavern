# dsh-tavern 保姆级使用教程（从零开始）

> 目标读者：完全没接触过 DSH 插件的新手。跟着做，10 分钟就能跑起第一个角色扮演会话。
> 配套插件：**dsh-tavern**（DeepSeek 酒馆管理面板），入口在 DSH「设置 → 通用设置 → 酒馆管理（原生）」。

---

## 目录

1. [这是什么 / 入口在哪](#1-这是什么--入口在哪)
2. [第一步：安装](#2-第一步安装)
3. [第二步：打开酒馆管理面板](#3-第二步打开酒馆管理面板)
4. [第三步：导入角色卡](#4-第三步导入角色卡)
5. [第四步：导入世界书](#5-第四步导入世界书)
6. [第五步：导入 / 管理预设](#6-第五步导入--管理预设)
7. [第六步：会话绑定（每个会话用不同的设定）](#7-第六步会话绑定每个会话用不同的设定)
8. [第七步：注入模式（全局 / 白名单）](#8-第七步注入模式全局--白名单)
9. [第八步：NSFW 成人模式](#9-第八步nsfw-成人模式)
10. [第九步：剧情选项（点击即发）](#10-第九步剧情选项点击即发)
11. [第十步：记忆总结（自动 / 手动）](#11-第十步记忆总结自动--手动)
12. [第十二步：角色关系网](#12-第十二步角色关系网)
13. [常见问题排查](#13-常见问题排查)
14. [数据存在哪 / 怎么备份](#14-数据存在哪--怎么备份)
15. [隐私说明](#15-隐私说明)
16. [许可说明](#16-许可说明)

---

## 1. 这是什么 / 入口在哪

dsh-tavern 是 DeepSeek Harness 的**原生酒馆管理面板插件**：把 SillyTavern 风格的角色卡、世界书、预设直接搬进 DSH，让每个 DSH 会话都能自动注入对应的角色设定。

**入口（重要）**：不在侧边栏！打开 DSH 的**「设置」→「通用设置」→「酒馆管理（原生）」** 就是它。面板里从上到下是：角色卡、世界书、预设、故事背景、记忆、关系网、剧情选项、NSFW 等区块。

## 2. 第一步：安装

**前置条件**：DeepSeek Harness v4.1.0 及以上，Web profile（`--profile web`）。

任选一种安装方式：

```bash
# 方式一：插件市场（推荐）
# DSH 设置 → 插件 → 搜索 dsh-tavern → 安装

# 方式二：npm
dsh plugin --profile web add dsh-tavern

# 方式三：GitHub
dsh plugin --profile web add github:chen731215-dev/dsh-tavern
```

装完**完全退出 DSH（含后台进程）再重新打开**。打开后若右侧/面板没出现，按 `Ctrl+Shift+R` 强制刷新一次。

> 老用户升级：直接重装最新版即可（数据都在 `~/.dsh/.agent-presets/`，升级不会丢）。

## 3. 第二步：打开酒馆管理面板

1. 点击 DSH 左下角 / 右上角的**「设置」**
2. 在「通用设置」分组最上方找到 **「酒馆管理（原生）」**
3. 点开即是面板，随 DSH 深色 / 浅色主题自动换肤，切到其它页面自动收起

## 4. 第三步：导入角色卡

支持 SillyTavern 格式角色卡：**PNG**（内嵌 chara 数据）或 **JSON**，可导入多份。

1. 在「角色卡」区块点**选择文件**（或直接拖文件进来）
2. 选中你的角色卡文件（.png 或 .json）
3. 导入成功后卡片会出现在列表里，可勾选启用
4. 角色卡自带的「角色世界书」会自动一起导入（不用手动分开导）

## 5. 第四步：导入世界书

1. 在「世界书」区块点**选择文件**，选 SillyTavern 世界书 JSON（多份可逐个导入）
2. 每条目可**独立开关**，点击条目可展开看内容
3. **注入模式二选一**（决定省不省上下文）：
   - **全文注入**：所有启用条目每次对话都注入（简单，但吃上下文）
   - **关键词触发（推荐）**：无关键词的"全局条目"（世界观/规则）每次注入；有关键词的"角色条目"只在对话命中关键词时注入 → **可省 60-70% 上下文**
4. 也可以点「＋ 新增条目」手动创建，或「从 MD 导入」用 Markdown 批量维护

## 6. 第五步：导入 / 管理预设

预设 = 一套完整的"角色卡 + 世界书 + 配置 + 记忆 + 关系网"组合包。

1. 在「预设」区块点**选择文件**，导入预设 JSON（RP 预设），可导入多份
2. 支持**创建 / 复制 / 重命名 / 删除**预设；「＋ 新建预设（复制当前）」可以基于当前配置开新档
3. 点**「💾 保存到 Harness」**，把当前启用的角色 + 世界书 + 预设模块合并生成 `agent.cordis.yml`

## 7. 第六步：会话绑定（每个会话用不同的设定）

这是本插件**区别于普通"全局设定"的核心功能**：每个 DSH 会话可以独立绑定一个预设，切换会话自动加载对应设定。

1. 打开一个会话，在面板里选好想要的预设
2. 点**绑定到当前会话**（或直接用右上角「🎭 预设名 ▾」浮动按钮切换）
3. 刷新页面 / 切换会话，绑定会自动记住（存在 `session-bindings.json`）
4. 发消息时，该会话会自动注入绑定预设的角色卡 + 世界书 + 记忆

> 想开新剧情？新建预设 → 绑定到新会话，互不干扰。

## 8. 第七步：注入模式（全局 / 白名单）

- **全局（global）**：所有会话都注入当前绑定预设 —— 适合就一个主设定
- **白名单（allowlist）**：只有勾选加入白名单的会话才注入 —— 适合多设定并存、不想串台

在「注入范围」区块切换；白名单模式下勾选要生效的会话即可。

## 9. 第八步：NSFW 成人模式

- 面板里有 **「🔞 NSFW 写作模式」** 开关，打开后：
  - 自动注入成人向写作提示（允许描写成人、色情、暴力等成熟题材，仅限虚构、角色均已成年）
  - 开启 NSFW 时自动联动 DSH 的成人模式
- 不想要随时关掉，开关独立于其它功能

### 写 NSFW 的正确用法（重要）

**NSFW 不是只开一个开关就完事**，必须两步搭配**一起使用**：

1. **在酒馆里改预设**：在「预设」区块导入/选用带成人词条的预设模块，需要时编辑里面的词条
2. **搭配改好的 agent 预设**：点 **「💾 保存到 Harness」** 生成 `agent.cordis.yml`，并在聊天顶部把 Agent 预设切换成**同名预设**（或点酒馆管理里的「⚡ 切到Agent预设」）

两者缺一不可：**酒馆预设负责提供 NSFW 词条与角色设定，agent 预设负责把破限与角色配置注入给 AI**。

- 只勾 NSFW 开关、不配预设 → 破限不完整
- 只改预设、没切到对应 agent 预设 → 注入不生效

## 10. 第九步：剧情选项（点击即发）

- 打开「剧情选项」开关后，AI 每次回复会额外输出 **3 个剧情选项**
- 点击选项 → **自动填入输入框并发送**，不用手动打字
- 选项下方有自定义输入框，想自由发挥就自己输入
- 独立开关，不开 NSFW 也能单独用

## 11. 第十步：记忆总结（自动 / 手动）

记忆 = 把"最近的剧情和状态"浓缩成一段文字，注入系统提示，让 AI 记得住前文。

1. 在「记忆模块」填一个 OpenAI 兼容接口（如 `https://api.deepseek.com/v1/chat/completions`，或你自己的中转地址）
2. 填 **API Key** 和**模型名**，点保存
3. **手动总结**：点「📝 立即总结」，立刻总结最近对话写入记忆
4. **自动总结**：打开开关，填"每 N 楼总结一次"（如 20），插件会按楼数自动总结
5. 总结时还会自动提取**角色关系**，更新关系网

> 不配 API 也能用角色卡 / 世界书 / 预设，记忆总结只是可选增强。

## 12. 第十二步：角色关系网

- 「关系网」区块显示总结时提取的角色关系**可视化图谱**（节点 + 连线）
- 预设级隔离：每个预设独立一套关系网
- 支持手动编辑 JSON、导出 / 导入

## 13. 常见问题排查

| 现象 | 解决 |
|---|---|
| 装完没有「酒馆管理」 | 完全退出 DSH（含后台进程）重开；还不行就 `Ctrl+Shift+R` 强刷 |
| 右上角预设按钮不显示 | 同上，强刷一次 |
| 手动总结报错 | 检查 API URL 是否带 `/v1/chat/completions` 路径、Key 和模型名是否正确（插件会自动补全路径，但建议填完整） |
| 不同会话设定串了 | 确认用了「会话绑定」，并把注入模式设为**白名单**，只勾选需要的会话 |
| AI 人设被别的插件盖掉 | 本插件注入顺序为 order 999（最后注入）+ 强制角色卡覆盖，若仍被盖，检查是否有其它记忆/召回插件在更后面注入 |
| 绑定重启后丢失 | 绑定存在 `session-bindings.json`，一般不会丢；若异常请检查该文件是否被清理工具误删 |
| 世界书关键词不触发 | 确认条目关键词字段（keys）已填写，且注入模式选的是「关键词触发」 |

## 14. 数据存在哪 / 怎么备份

所有数据都在本机，**不经过任何云端**：

```
~/.dsh/.agent-presets/
├── presets.json              预设列表
├── session-bindings.json     会话绑定
├── tavern-state.json         全局状态（注入范围 / NSFW / 剧情选项 / 记忆配置）
├── edited-messages.json      修改过的 AI 回复
└── <预设目录>/
    ├── agent.cordis.yml      角色卡（生成给 DSH 的预设）
    ├── characters.json       角色卡列表
    ├── worldbooks.json       世界书
    ├── presets.json          预设词条
    ├── memory.md             记忆
    └── relations.json        关系网
```

**备份**：直接复制整个 `~/.dsh/.agent-presets/` 文件夹即可（或先退出 DSH 再复制更稳妥）。还原：把文件夹放回去。

## 15. 隐私说明

- 角色卡 / 世界书 / 预设 / 记忆 / 关系网**全部只存本机**，插件没有任何遥测、埋点或上报
- 客户端所有请求都发往**本机 DSH 服务器**（`/api/tavern/*`），不连任何外部服务
- 唯一可能外发的数据：**记忆总结**时，把最近对话发给你**自己配置的** OpenAI 兼容 API（默认关闭、不配置就不发）

## 16. 许可说明

本项目采用 **CC BY-NC-SA 4.0**（署名-非商业性使用-相同方式共享）协议，免费供个人 / 学习 / 非商业使用；**禁止商业用途**，转载需注明来源（作者 chen731215，仓库 https://github.com/chen731215-dev/dsh-tavern）。详见 [LICENSE](./LICENSE)。

---

> 遇到教程没覆盖的问题，欢迎到仓库提 issue：https://github.com/chen731215-dev/dsh-tavern/issues
